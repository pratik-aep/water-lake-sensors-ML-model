# Linux server: add with `crontab -e`. Replace /PATH/TO/h20_quality_detection; alert settings go in the environment.
# Every morning at 06:30: take in new exports, refresh weather, report, dashboard, alerts.
30 6 * * *  cd /PATH/TO/h20_quality_detection && .venv/bin/python -m models.pipeline.daily --notify >> models/pipeline/daily.log 2>&1
# The 1st of each month at 02:00: retrain every model on all history gathered so far. Every path is given
# explicitly: train_all's defaults point at the synthetic demo data. Missing files just skip their models.
0 2 1 * *  cd /PATH/TO/h20_quality_detection && .venv/bin/python -m models.pipeline.train_all --readings models/pipeline/history/readings.csv --air-readings models/pipeline/history/air_readings.csv --weather models/pipeline/history/weather.csv --lab models/pipeline/history/lab_results.csv --known-episodes models/pipeline/history/maintenance_log.csv --vendor-mapping models/pipeline/vendor_mapping.json >> models/pipeline/retrain.log 2>&1
